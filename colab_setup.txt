!pip install -q transformers datasets tensorboard accelerate

from google.colab import files
files.upload()  # sobe nano_pt.py
files.upload()  # sobe tokenizer-arpa-32k.zip

import zipfile
with zipfile.ZipFile("tokenizer-arpa-32k.zip", "r") as z:
    z.extractall(".")

exec(open("nano_pt.py").read())
