!pip install -q transformers datasets tensorboard accelerate

from google.colab import files
files.upload()  # seleciona nano_pt.py + tokenizer.json + tokenizer_config.json juntos

import os
os.makedirs("tokenizer-arpa-32k", exist_ok=True)
os.rename("tokenizer.json", "tokenizer-arpa-32k/tokenizer.json")
os.rename("tokenizer_config.json", "tokenizer-arpa-32k/tokenizer_config.json")

exec(open("nano_pt.py").read())
